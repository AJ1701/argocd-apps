apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: operators
  namespace: argocd #Where ArgoCD lives
spec:
  project: default
  source:
    repoURL: https://github.com/AJ1701/argocd-demo-dev
    targetRevision: main
    path: operators
    helm:
      valueFiles:
        - crc-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-operators #Destination of the apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
